# audio-graph-settings

AudioGraph's product settings: the per-user `config.json`, plugin folders,
pinned module paths, and the adjacent derived catalogue path. The crate keeps
the existing JSON format and first-run/default restoration behavior.

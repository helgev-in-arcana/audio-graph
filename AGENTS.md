# AGENTS.md

このリポジトリで作業するときの指示。

## 設計と実装に関する指示の解釈について

第一選択として、 AGENTS.md とセッション中の指示を守ること。

ただし、要求を満たせない、具体的な不具合が生じる、変更が不必要に大きくなるなどの問題がある場合、その他より良い方針が存在する場合には、最小限の例外を提案することを認めます。
例外を提案する際は、以下を簡潔に示し、私の明示的な承認を得てから逸脱する変更に着手してください。

- 該当する指示と、従うことで生じる具体的な問題
- 提案する最小限の代案と、その影響・不利益

承認前でも、指示に反しない範囲で、判断に必要な調査や例外の判断に依存しない作業は進めて構いません。

この節は、明示的な禁止事項・承認条件や、上位の指示・権限制約を緩和するものではありません。

## レビュー可能な変更を重視すること

- 変更は、レビュー担当者が影響を予測できる単位にまとめる。
  行数の少なさより、同時に理解する必要がある設計判断や
  挙動変更の少なさを重視する。
- 各変更単位には一つの明確な目的を持たせ、無関係な改善を混ぜない。
- 必要に応じて、準備リファクタ、機械的な名前変更・コード移動、
  挙動変更を分離する。PRの分離が不自然ならコミットを分ける。
- 準備変更は今回の目的に必要なものに限定する。
  分割を理由に、汎用化・抽象化・互換層を追加しない。
- 分割した単位は、可能な限り単独で安全性を説明・検証できるものにする。
  強く結合した変更を形式的に細分化しない。
- 機械的な変更や生成物による大きな差分は許容するが、
  挙動変更とは区別して説明する。
- 大きな変更を一体で行う必要がある場合は、実装前に
  その理由とレビュー方法を説明する。

## 設計に差し戻す判断

物差しは一つ、**この苦労は再発するか**。同じ形の要求がもう一度来るなら、設計は今回の分だけでなく将来の分も請求してくる。一度きりなら押し通したほうが安い。

### 止まる目安

**同じ壁に二度ぶつかった、二度書き直した、最初の見立ての倍を超えた** — どれかに当たったら手を動かすのをやめる。一度目の摩擦で止まるのは早すぎるし、半日殴り続けるのは遅すぎる。

重さの兆候は、要求の重さと変更の重さが釣り合っていないこと。一文で言える要求なのに、離れた三箇所へ同じ形の分岐を書き足している。既存のシグネチャにこの要求専用のフラグを通している。テスト本体より足場のほうが長い。

### 止めるのは手であって仕事ではない

黙って設計を作り替えるのも、黙って押し通すのも取らない。前者は頼まれていない大きさの変更で、後者は動くものと引き換えに請求書を隠す。

判断に依存しない部分は先に終わらせておく。迂回路で一度動かして確かめるのはよい — 見立ての精度が上がる。ただしそれをコミットして「終わった」ことにはしない。

### 報告に書くこと

四つ。長さは画面一つぶんでいい。

1. **症状。** 何をしようとして、差分がどんな形になったか。ファイル名と箇所数まで具体的に。
2. **原因。** どの抽象がこの要求を表現できないのか。「`X` は〜という前提で作られているが、この要求はその前提の外にある」という形で。症状の言い換えにしない。
3. **選択肢。** 二つか三つ。それぞれの費用と、それを選んだあと何が簡単になるか。**「このまま押し通す」を必ず含める** — 費用を並べてみればそれが最善のこともある。
4. **推奨。** どれを選ぶか、なぜか。

決められないから聞くのではない。この設計とどれくらい長く付き合うのか、この要求は今後も来るのか — 決める材料の一部をこちらが持っていないから聞く。

### 差し戻さないとき

面倒なだけ、量が多いだけなら、それは設計の問題ではない。手を動かす。

まだコードを読み切っていないだけ、ということも多い。理解不足を設計のせいにしない。読んでから判断する。

動いているものをより綺麗にしたい、というだけの理由でも止めない。

## コメントの作法

既定は、書かないこと。以下を満たすときだけ書く。
コメントは**英語**で書く（コミットメッセージも英語。`docs/` 上のやり取りは日本語）。

### 1. コンテキストレスであること

コメントはそれ単体で機能しなければならない。読む人は差分もPRも会話も見ていない。

- **変更履歴を書かない。** 「以前は」「かつて」「〜だった」「〜に変わった」「古い実装では」は禁止。
- **代わりに性質を書く。** 反例が説明として要るなら、過去形ではなく条件法で書く — 「こうしないとどうなるか」。

```rust
// Bad: The note ops used to run twice for every sub-block, into buffers
// cleared before each replay. That copied every event twice per note op.

// Good: The note ops run once for a sub-block, appending to what the buffers
// already hold. Replaying them per reader instead, into buffers cleared before
// each replay, would copy every event twice per note op.
```

### 2. ローカルな事情に言及しない

PR番号、Issue番号、ブランチ名、`memo/` の文書、「今回の変更」「先ほどの議論」「ユーザーの指摘」— どれもコメントに書かない。それらはコミットメッセージと Issue の仕事。

`TODO` / `FIXME` も置かない。やり残しは Issue へ。

### 3. 実在するものだけを名前で指す

型・関数・ノード名を消したら、それを名指しているコメントも同時に直す。削除された `Expression` ノードへの参照が3箇所生き残っていた、という類の事故が起きやすい。

同じファイル・同じクレート内のシンボルを指す `` `foo` `` や `[`Bar`]` は歓迎する — それはローカルな事情ではなく、読み手が辿れる場所だから。

### 4. What ではなく Why

コードを読めば分かることは書かない。書くのは、**なぜ他の選択肢ではないのか**。

```rust
// だめ: Push the event onto the buffer.

// よい: Appends unless the buffer is full. Dropping an event is bad; growing a
// `Vec` on the audio thread is worse.
```

数字にはその数字である理由を添える。定数の doc には根拠を書く（「サブブロック1個より短くできない、quantum 32・48kHz で約0.67ms」のように、単位と実際の値まで）。

### 5. テストの doc comment は主張を書く

`#[test]` の doc は、そのテストが何を**守っている**かを一文で述べる。手続きの説明ではない。壊れたときに何が壊れたのかが、その一文で分かること。

```rust
/// The loop pays for itself and nothing else does.
///
/// A patch is one canvas: a feedback delay in a corner of it must not put
/// every plugin in the program on sub-block granularity.
#[test]
fn a_loop_does_not_drag_the_rest_of_the_patch_down_with_it() {
```
